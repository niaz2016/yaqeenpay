# Cloudflare Tunnel Configuration for YaqeenPay on techtorio.online
# Copy this content to: C:\Users\Precision\.cloudflared\config.yml

tunnel: niaz
credentials-file: C:\Users\Precision\.cloudflared\0c9e15ed-9dfa-47d3-a7b1-5f1153757b7e.json

ingress:
  # Main YaqeenPay application
  - hostname: techtorio.online
    service: http://localhost:8080
  
  # API subdomain for mobile apps or external integrations
  - hostname: api.techtorio.online
    service: http://localhost:8080
  
  # Admin panel subdomain (optional, for separate admin access)
  - hostname: admin.techtorio.online
    service: http://localhost:8080
  
  # Catch-all rule (required - must be last)
  - service: http_status:404